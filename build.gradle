plugins {
    // 8.9.1+ required by androidx.core:core:1.17.0; also tested with compileSdk 36
    id 'com.android.application' version '8.9.1' apply false
}

// Shared SDK + library versions for all modules
apply from: 'variables.gradle'
